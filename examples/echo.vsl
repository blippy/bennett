\ added by mcarter 2020-06-30
\ test of syscalls
\ this just echos stdin to stdout

loop:
	SYS 0 \ getchar
	SUB R0,R15 \ get the flags
	BGTZ fin \ pos => EOF, so exit
	SUB R0, R15 \ convert back to positive
	TRAP
	BRA loop
fin:
	HALT

